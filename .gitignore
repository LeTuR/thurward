# Editor / OS junk
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/
*.tmp

# Rust (forward-looking; no src/ yet)
target/

# Node tooling (CI / local validators)
node_modules/

# Local secrets / env
.env
.env.*
!.env.example

# Bench harness artifacts (also gitignored locally under bench/.gitignore;
# duplicated here so a bare `git status` from the repo root behaves).
bench/images/
bench/keys/
bench/results/smoke/
bench/results/b-*/
